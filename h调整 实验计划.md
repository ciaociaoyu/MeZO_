- # h 探究实验计划

  ## 实验目标与约束说明

  本实验的总体目标是在**不破坏原有整体结构与实验意图的前提下**，完成对现有 h 调整相关实验方案的整理与重构，使其更加适合在 Typora 等 Markdown 编辑器中进行长期维护、阅读与迭代。在整个修改过程中，始终遵循“最小改动”原则，不引入新的实验假设，不改变原有变量含义，也不对实验逻辑进行重排，仅对表达形式与段落结构进行优化，从而提升可读性与连续性。

  具体约束包括：第一，不改变任何 public API 层面的含义或接口约定；第二，不重排原有实验流程中的关键步骤顺序；第三，除非为表达清晰所必需，不对变量命名、符号定义或术语进行替换；第四，如需补充说明，仅以局部文字解释的方式嵌入，不进行穿透式结构调整。

  ## 实验整体设定

  实验基于一条已经选定并正常运行的训练 run 进行，在不干扰原始训练主流程的前提下，周期性地插入探针（probe）操作，用于分析不同 h 取值下有限差分梯度估计的误差特性以及其对 loss 的即时影响。探针并不改变模型的长期训练轨迹，其产生的参数扰动在每次探针结束后都会被完整回滚。

  探针频率由一个固定的步长 K 控制，例如每 K=200、500 或 1000 个 training step 触发一次探针。在每一次探针中，都会使用一个**固定的 probe batch**，记为 $B_{\text{probe}}$，以确保在同一次探针内部，不同 h 值之间的比较只反映数值差分尺度本身的影响，而不混入额外的 batch sampling 噪声。

  此外，每次探针还会采样一组方向向量集合 $\{u_1, \dots, u_M\}$。这些方向在每一次新的探针触发时可以重新采样，但在**同一次探针内部**，对于所有 h 取值，始终复用同一组方向，以保证横向比较时的变量控制。通常建议对这些方向进行单位范数归一化，以便将差分结果解释为方向导数意义上的估计。

  ## h 的取值范围

  扰动尺度 h 采用对数刻度进行设置，覆盖从极小扰动到常用经验尺度的一个宽区间。一个典型的取值集合可以写为：
  $$
  h \in \{10^{-8},\; 3\cdot 10^{-5},\; 10^{-4},\; 3\cdot 10^{-4},\; 10^{-3},\; \dots,\; 10^{-2}\}.
  $$
  这种设置的目的在于同时覆盖数值截断误差主导区间与随机噪声主导区间，从而在实验层面观察不同误差源随 h 变化的整体行为。
  
  ## 单次探针的执行流程
  
  对于任意一个触发探针的训练 step $t$，探针的执行流程可以概括为以下几个连续阶段。首先，需要完整保存当前模型参数 $\theta_t$，以确保探针过程中产生的所有参数扰动都不会影响后续的真实训练。
  
  接下来，在固定的 $B_{\text{probe}}$ 上计算两个基准量：一是当前参数下的 loss 值 $L(\theta_t)$，二是对应的真实梯度 $g_{\text{true}} = \nabla_\theta L(\theta_t; B_{\text{probe}})$。这里的真实梯度通过一次标准反向传播获得，仅用于探针分析，不参与真实参数更新。
  
  在获得真实梯度之后，采样 $M$ 个方向向量 $u_j$。随后，对每一个候选 h 值，依次在这些方向上进行中心差分计算。具体而言，对于每个方向 $u_j$，需要执行两次前向计算，分别得到
  $L^+ = L(\theta_t + h u_j)$ 与 $L^- = L(\theta_t - h u_j)$，并由此构造方向导数的有限差分估计
  $$
  d_{\text{fd},j}(h) = \frac{L^+ - L^-}{2h}.
  $$
  与此同时，可以利用真实梯度计算对应的真实方向导数
  $$
  d_{\text{true},j} = g_{\text{true}} \cdot u_j,
  $$
  并记录二者之间的误差
  $$
  e_{d,j}(h) = d_{\text{true},j} - d_{\text{fd},j}(h).
  $$
  这些误差项将作为后续分析中研究 h 与梯度估计偏差关系的重要观测量。

  ## h 与 loss 关系的即时评估

  除了分析方向导数误差之外，还可以在单次训练过程中直接评估不同 h 所对应的“一步更新效果”。具体做法是引入一个**虚拟的一步更新**过程，而不真正修改训练参数。首先选定一个统一的学习率 $\eta$，通常直接使用当前训练配置中的 learning rate。

  然后，对于每一个 h，根据对应的有限差分梯度估计 $g_{\text{fd}}(h)$ 构造一个临时参数
  $$
  \theta'(h) = \theta_t - \eta \, g_{\text{fd}}(h).
  $$
  在同一个评估 batch $B_{\text{eval}}$（可以与 $B_{\text{probe}}$ 相同，也可以单独指定）上计算
  $$
  \Delta L(h) = L(\theta'(h)) - L(\theta_t).
  $$
  该量可以被直观理解为：在当前训练状态下，使用尺度为 h 的梯度估计进行一次更新，理论上会带来多大的 loss 改变。因此，$\Delta L(h)$ 为分析 h 对训练即时收敛行为的影响提供了一个直接、可解释的指标。

  ## 日志与结果记录

  所有与探针相关的中间量与统计结果，包括方向导数误差、不同 h 下的 $\Delta L(h)$，都应同时记录在标准日志输出与结构化的记录文件中，以便后续进行系统性汇总、可视化分析以及跨实验对比。这一记录策略对于支撑后续理论分析与实验结论至关重要。