# **h调整 实验计划**

全文分为三层：

1. 实验前准备（保证后面不翻车）
2. 四个“可被验证的命题”（你真正要证明的东西）
3. 两条实验主线：探究式（机理）与实用式（方法）

## **一、实验前准备（非常重要）**

### **1. 让** **run** **能“干净地结束”**

你之前遇到的 best_model_ckpt 不存在导致程序在最后阶段报错退出，本质上不是模型问题，而是**工程设置不适合做系统实验**。

对于接下来这种“多 seed、多配置”的实验：

- 建议直接关闭 evaluate_during_training
- 或在 run.py 中对 best_model_ckpt 加 guard

目标只有一个：

> **即使训练效果一般，也要保证每个 job 都能完整产出日志和中间结果。**

否则你会发现：模型其实已经跑完了，但因为最后一行红字，统计时反而缺数据。



------





### **2. 统一输出目录结构（这是救命的）**



你已经开始用 job name 命名输出目录，这个方向是完全正确的，而且必须坚持。

后面每一个实验配置，都应该**天然对应一个独立目录**，并且至少包含：

- 训练/评估指标（metrics.json / eval_results）
- h probe 相关输出（hprobe*.jsonl）
- 完整 .out 日志

你后期画图、做统计、对比 seed 的时候，本质上是在做“数据分析”。**没有稳定的目录结构，后期基本不可能对齐数据。**



## **二、你真正要证明的四个命题**



为了避免陷入“感觉没有最优解”的主观叙事，我们把目标明确写成四个**可以被实验验证的命题**。这四个命题几乎可以直接作为论文中 Section 的逻辑骨架。

### **命题 1：单一最优 h 在现实训练中不稳健**

在存在噪声（batch 随机性、有限差分噪声、非凸性）的情况下：

- 所谓的“最优 h”会随着 seed、训练阶段、batch 条件发生漂移
- 在合理范围内，性能往往呈现平台状，使得“最优点”在统计上难以可靠辨识

换句话说：

> **即使你愿意付出 sweep 的成本，得到的“最优 h”也不具有可复用性。**



------





### **命题 2：区间比单点更合理**

与其追求一个精确的 h*，更合理的目标是：



- 找到一个随训练状态变化的**可行区间** [h_\text{floor}, h_\text{right}]
- 并且用**低频、低开销**的方式，在训练中维持 h 落在这个区间内





这一定义直接把问题从“精调参数”转化成了“维持稳定性”。



------





### **命题 3：噪声越大，区间方法越必要**



在更噪声的条件下（例如更小 batch、更低精度）：

- 可行区间会变窄，甚至整体移动
- 经验 h 更容易越界或导致训练不稳定
- 区间/自适应方法的收益和必要性显著增强，尤其体现在 worst-case



这条命题是你方法**存在价值**的核心来源。

------





### **命题 4：h 与噪声之间存在可观察的规律**

虽然不追求严格闭式理论，但实验上可以观察到：

- 噪声上升时，h_floor 往往右移（太小的 h 不可分辨）
- 同时 h_right 变得更不稳定，甚至收缩
- 可行区间宽度与噪声指标之间存在可拟合的经验关系

这使你的工作不只是“一个 trick”，而是**有机制解释的现象总结**。





## **三、两条实验主线的整体思路**





整套实验被刻意拆成两条互补但目标不同的主线：



- **主线 A：探究式（机理）**

  

  - 目标：解释为什么“没有稳定最优 h”，以及这种不稳定性的结构

  

- **主线 B：实用式（方法）**

  

  - 目标：证明你的方法解决的是“降低调参成本和失败率”，而不是“提升 0.3%”

  





这两条线在论文中可以分别对应“分析”与“方法验证”。



------





## **四、主线 A：探究式（机理线）**







### **A1. 证明“最优 h 会随训练阶段漂移”**





这是整篇工作中**最关键的一组实验**。



做法是只使用一步收益的 Probe（Probe 3），在不同训练阶段采样 dL_{new}(h) 曲线。为了保证路径一致性：



- 训练过程中固定使用一个 h_train（例如你目前的默认经验值）
- 只在 probe 时扫描 h

在多个 global step（例如 0 / 1000 / 2000 / 3000）以及多个 seed 下，记录：

- 每个阶段的收益曲线
- 对应的 h_gain（最优位置）



你最终需要展示的不是“某个 h 最好”，而是：

> **h_gain 随训练阶段明显漂移，且曲线存在宽平台。**

### **A2. 把“最优点”变成“可行区间”**

这一部分用的是 Probe 1/2（边界诊断），你已经基本跑通。

核心目标是：

- 从 probe 输出中提取 h_floor 与 h_right
- 观察它们随训练 step 的变化轨迹

最终你可以画出：

- h_floor(step)
- h_right(step)
- 区间宽度（如 h_right / h_floor）

这一步直接把抽象的“感觉不稳定”，变成了**清晰可视化的区间演化过程**。

### **A3. 用统计方式否定“单点最优”的可靠性**

为了避免被质疑“只是你觉得平台宽”，这里需要一个非常克制但有力的统计实验。



在一个中等噪声设置下：



- 选取 5–7 个 h，覆盖可行区间及明显越界点
- 每个 h 跑多个 seed
- 比较最终指标的均值与方差

你希望看到并展示的是：

> 在可行区间内，不同 h 的性能差异小于跨 seed 的方差。

这句话一旦被图和误差条支撑，基本无法反驳。



------





## **五、主线 B：实用式（方法线）**





如果说主线 A 回答的是“为什么”，那主线 B 回答的就是“那该怎么办”。





### **B1. 把你的方法定义成“成本可控的版本”**





你需要非常明确地告诉读者：



- 你的方法不是“每一步都做重 sweep”
- 而是低频、低方向数、低点数的区间维护





通过简单的 ablation（更新频率、方向数），你可以画出：



- 性能 vs 额外开销 的 Pareto 曲线





这一步的意义在于：



> **证明你不是用算力换稳定性，而是用结构换稳定性。**



------





### **B2. 对比对象要选对**





最有说服力的对比不是“只和经验值比”，而是四类：



- 经验固定 h
- 理论给定 h
- Oracle sweep（上界，但不可用）
- 你的方法





你强调的重点应该是：



- 接近 oracle，但不需要 sweep
- 稳定性更好，失败率更低





------





### **B3. 高噪声压力测试（必要性证明）**





通过改变 batch size 和精度，你几乎必然会看到：



- 低噪声条件下：经验值也能工作（平台宽）
- 高噪声条件下：固定 h 极其敏感，而你的方法更稳





这一步是整篇故事中“为什么现在需要你方法”的最佳注脚。



------





## **六、从实验中提炼经验规律**





最后一步不是再堆实验，而是**把已有 probe 输出再榨一次价值**。



通过将噪声代理指标与 h_floor / h_right 对数作图，你可以总结出：



- 单调趋势
- 大致斜率
- 不同噪声配置下的对比





这会让你的工作非常像一篇“机制导向”的论文，而不只是工程技巧。



------





## **七、关于 h sweep：需要，但要克制**





你仍然需要做少量 sweep，原因很简单：



> 没有对照，就无法证明“最优点不稳健”。



但 sweep 的目的不是找最优，而是：



- 证明平台存在
- 证明最优点不稳定
- 证明 sweep 成本高但收益不确定





两个场景、每个 5–7 个点、2–3 个 seed，已经完全足够支撑论点。



------





## **八、最小可行实验集（MVP）**





如果时间或算力有限，只要完成以下五步，你就已经拥有一条完整故事线：



1. Probe 12 边界轨迹（中等噪声）
2. Probe 3 分阶段收益
3. 一个中等噪声下的粗 sweep
4. 一个高噪声下的固定 h vs 你方法
5. （可选）高噪声下的粗 sweep





做到这里，你已经可以非常自信地回答：



- 为什么没有稳定最优 h
- 为什么区间更合理
- 为什么你的方法有必要
- 以及为什么经验值“有时也能用”