- 我会把原始文件内容发你，请把结果以“真实可下载文件”形式交付

  

  目标：在不破坏原有结构的前提下完成【目标】。

  约束：

  \- 最小改动：不改 public API、不重排代码、不做格式化。

  \- 除非必要，不要改变量/函数命名；不要移动函数位置。

  \- 如果需要新增代码，尽量局部新增，避免穿透式改动。

  

  输出格式（必须严格遵守）：

  1) 先给出 unified diff（用于我审阅）。
  2) 再给出“修改后的完整文件”

  

  以下是要求：

  请基于目前的probe1，2，3做改动，删除原有的功能，增加下面的新功能，但是尽量保持传递的参数名字不变。你可以视情况删除probe1 2 3原有的代码，函数，或者是整合。

  选定一条训练 run

  ​	•	设定探针频率：例如每 K=200/500/1000 step 做一次探针

  ​	•	每次探针用：

  ​	•	固定的 probe batch：B_{\text{probe}}

  ​	•	固定方向集合：u_1,\dots,u_M（每次探针重新采样，但在“同一次探针内”对所有 h 复用同一组 u 以控制变量）

  

  h 的取值：

  ​	•	用对数刻度：h \in \{10^{-8}, 3\cdot 10^{-5}, 10^{-4}, 3\cdot 10^{-4}, 10^{-3}, \dots\, 10^{-2}}

  每个探针 step t 的执行步骤

  ​	1.	保存当前参数 \theta_t（用于探针结束后恢复）

  ​	2.	在 B_{\text{probe}} 上计算：

  ​	•	当前 loss：L(\theta_t)

  ​	•	真实梯度：g_{\text{true}}=\nabla_\theta L(\theta_t; B_{\text{probe}})（一次反传）

  ​	3.	采样 M 个方向 u_j（建议单位范数，便于解释方向导数）

  ​	4.	对每个 h\in H：

  ​	•	对每个方向 u_j：

  ​	•	前向两次：L^+ = L(\theta_t+h u_j), L^- = L(\theta_t-h u_j)

  ​	•	方向导数差分：\; d_{\text{fd},j}(h)=\frac{L^+ - L^-}{2h}

  ​	•	真实方向导数：\; d_{\text{true},j}=g_{\text{true}}\cdot u_j

  ​	•	记录误差：\; e_{d,j}(h)=d_{\text{true},j}-d_{\text{fd},j}(h)

  ​	5.	探究 h–loss（单次训练内也能做）：做“虚拟一步更新”评估

  ​	•	设定一个统一学习率 \eta（取训练里的 lr ）

  ​	•	对每个 h，计算临时参数：

  \theta'(h)=\theta_t-\eta\, g_{\text{fd}}(h)

  在同一批 B_{\text{eval}}（可与 B_{\text{probe}} 同批或另取）上算

  \Delta L(h)=L(\theta'(h)) - L(\theta_t)

  ​	•	记录 \Delta L(h)，它就是“该 h 的梯度估计带来的一步 loss 改变量”

  

  对于记录部分，请同时在log和记录文件中都记录