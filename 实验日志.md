### 1.13实验暴露的核心问题（Adaptive-h / ν₃ 估计）

**问题 1：估计 ν₃ 的 h 不断变大，最终跑到非局部尺度**
- **现象**：`estimate_nu3` 中的 `chosen_h` 经常增大到 `0.05 ~ 0.5`。
- **根因**：
  - h 的起点来自 `eps_f^{0.2}`，本身偏大；
  - 扫描策略只向上扩展，未优先恢复局部性；
  - 当没有任何 h 同时通过 (18a)(18b) 时仍强行 fallback 选 h。
- **后果**：在完全非局部尺度上估计 ν₃，Taylor 展开前提失效。

**问题 2：prox 指标长期偏大（常 > 0.5）**
- **现象**：日志中 `prox ≈ 0.4 ~ 0.9`，几乎从不满足 `prox <= 0.1`。
- **含义**：
  - 扰动 ±h 后，loss 相对原点变化达 40%~90%；
  - 说明 h 已远离“局部扰动”范围。
- **后果**：在该尺度上估计的 ν₃ 不具备物理意义。

**问题 3：Δ3 = 0 → ν₃ = 20 的 fallback 导致 h 被“顶飞”**
- **现象**：当 `Δ3(h)=0`（数值地板）时，将 `ν₃` 强制设为 20。

- **根因**：
  - `ν₃` 在公式 `h ∝ (ε_f / ν₃)^{1/3}` 中位于分母；
  - 将 ν₃ 设为较小常数会直接放大 h。
  
- **后果**：单个异常 direction 即可把 `h_raw` 推到过大尺度，导致自适应 h 失控。

  ### <span style="color:red">好的一方面是，现在能收敛了，这说明没有很离谱的错误</span>

  TODO 可能我现在是顺序取样，有潜在的出事风险
  这是一个潜在，但很重要的坑，因为我另一个实验也用的这玩意
  
  **要记录下来！！！！**

## 实验阶段性分析（2026-01-13）

**总体结论**：  
本次实验整体运行稳定，之前出现的三类核心问题（非局部 h、prox 过大、Δ3=0 顶飞 h）均未再系统性出现，  
说明当前的 **ν₃ 尺度检测 + 双向扫描 + 无效方向丢弃 + EMA 平滑更新** 逻辑是有效的。

---

### 1. Adaptive-h（训练用 ε）轨迹
- 初始：`h0 = 1e-2`
- 随训练逐步下降并收敛：
  - 最终：`h_ema ≈ 3e-5`
  - 与理论估计 `h_raw ≈ 3e-5` 基本一致
- log-EMA（β=0.1）确保 h 更新为慢变量，未出现突发震荡

---

### 2. ν₃ 估计的局部性（prox）
- 所有被接受的 `chosen_h` 均满足：
  - `prox_max < 0.1`（最大约 0.098）
  - 中位数约 `1e-3` 量级
- 说明：**ν₃ 始终在“局部扰动尺度”上估计**
- 之前常见的 `prox > 0.5`（严重非局部）问题未复现

---

### 3. h 过大 / 过小的自动识别机制
- **h 太大（非局部）**：
  - 早期多次出现 `trial-0 prox_ok=False`
  - 通过向下扫描（h 缩小）成功恢复局部性
- **h 太小（噪声主导）**：
  - 后期出现 `trial-0 snr_ok=False, prox_ok=True`
  - 触发向上扫描（h 放大），成功通过 (18a)(18b)
- 结论：当前策略已具备 **双向调节 h 的能力**，不再单向“越调越小”

---

### 4. Δ3 = 0 的数值地板问题
- 本次实验未出现 “Δ3=0 → ν3=20 → h 顶飞” 的旧 fallback
- 仅少数 step 触发：
  - `[estimate_nu3][retry] use larger local h=...`
  - retry 后均得到合理 ν₃（未放大 h）
- 数值地板问题已被有效限制为局部扰动

---

### 5. 训练与验证表现
- Dev 集性能在 `~30k step` 达到最优（acc ≈ 0.468）
- 随后继续训练 dev acc 下降
- 最终 Validate/Test 通过 **reload best checkpoint** 恢复到最佳性能
- 说明：当前设置下 **过长训练可能引入漂移**

---

### 6. 仍需注意的问题
- 训练仍使用 `SequentialSampler`（未 shuffle）：
  - 可能引入数据顺序偏置
  - 对 adaptive-h 与 ν₃ 稳定性存在潜在影响
- 建议后续进行 `shuffle=True` 的对照实验

  - > [!IMPORTANT]
    >
    > 千万要记得设置一个seed

- 还需要考虑在截断误差前乘一个系数，适当放大h，让h可以收敛地更好。因为我发现计算出来的h并不是一个最优值。（人为缩放一下会不会更好？）
- 还有一个想法，如果一开始给h一个异常值，他能不能自己回到正常区间？
- 我的研究到底有啥意义，我迷糊了都。我需要去找到最佳的h吗，这个最佳指的是收敛速度和精度吗？我需要去测量收敛速度吗？

# 实验进展（2026-01-15｜One-page）

## 研究目标（当前阶段）
本阶段目标不是追求完整性能最优，而是：
- 澄清 **h / eps / probe / 取样机制** 的作用机理
- 用**最小规模实验**验证：  
  **通过调整 eps + adaptive-h，可以带来更合理、更稳定的训练行为**

---

## 一、probe 机制的关键理解（已澄清）

- **probe 1**：偏向寻找局部稳定的最小尺度（保守）
- **probe 2**：偏向测试更大的尺度（检验 h 是否过小、是否被截断误差主导）
- probe 并非重复测量，而是在 **不同尺度假设下探测 h 的合理区间**

参数理解：

H_PROBE=1, H_PROBE_MODE=3

H_PROBE_EVERY=100, H_PROBE_NB=2

- 从 step ≥ 100 开始
- 每 100 step 触发一次 probe
- 每次 probe 执行 2 次测量（不是每一步都测）

---

## 二、eps / h 选择的实验性结论（重要）

- **eps = 1e-2**：
  - 易引入较大截断误差
  - 导致 probe 统计量（如 nu3）异常放大
  - ❌ 不适合作为稳定 baseline
- eps 是一个 **会实质影响更新质量的可调参数**，而非固定常数

结论：
- fixed-h 实验 **不再需要系统性重跑**
- 仅保留：
  - 一个经验 h（如 1e-3）作为 reference
- 主要算力应集中在：
  - adaptive-h
  - eps 调整带来的趋势对比

---

## 三、取样机制的进展（Rolling Sampling）

### 动机
- 原始 probe / h 估计若始终基于 **固定或高度相关 batch**
  - 容易引入偶然噪声
  - 影响 h 稳定性判断

### 当前进展
- 已开始将 probe / h 估计中的 batch 机制：
  - 从“固定 batch / 单次采样”
  - 转向 **滚动取样（rolling sampling）**
- 核心思想：
  - 每次 h / probe 估计使用 **不同但相邻的 batch**
  - 在不引入全局随机性的前提下，降低估计方差

### 状态
- 代码层面已完成初步修改与修复
- 当前重点是：
  - 验证滚动取样是否带来：
    - 更平滑的 h 变化
    - 更合理的 probe 统计量分布

---

## 四、工程与日志管理（现状）

- 当前实验结果统一输出至 `result/` 目录
- 希望支持：
  - 当前目录输出
  - 使用 job name 作为结果文件夹名
- `run.py` 中暂未直接支持（已确认现状，待后续实现）

---

## 五、当前“最小阶段性成果”定义

在 SST-5 上：
- 对比：
  - fixed-h（少量参考）
  - adaptive-h + eps 调整
- 展示：
  - h 选择更稳定
  - probe 统计量更合理
  - 训练行为更一致

→ 作为与导师讨论的 **第一阶段证据**

---

## 六、一句话总结

> 本阶段的核心进展不在于“跑了多少实验”，  
> 而在于 **明确了哪些机制在影响训练，以及哪些实验值得继续投入算力。**
